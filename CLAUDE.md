# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

MultiLED is an embedded C/C++ project for controlling multiple LEDs. The project is in its initial stages.

## Repository Structure

- `设计文档.md` — Design document (Chinese), currently empty
- `.gitignore` — Configured for C/C++ build artifacts (object files, libraries, executables, kernel modules)

## Build

No build system has been configured yet. The `.gitignore` suggests the project will produce C/C++ compiled artifacts (`.o`, `.elf`, `.exe`, `.hex`).
